# Redis wrapper for AHK

This is a Redis wrapper for AHK. It provides a set of functions to interact with Redis in AHK scripts.

## Usage

1. Install Redis server on your machine.
2. Start the Redis server.
3. Run the demo